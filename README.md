# Advanced NLP Final Project

This repository contains the code, notebooks, data artifacts, and saved models for the final Advanced NLP project.

## Project Layout

- `scripts/setup.py`: downloads the source dataset, filters the two target categories, and creates `data/trustpilot_reviews.pkl` and `data/filtered_reviews.csv`.
- `scripts/setup_part3.py`: creates the Part 3 training-size CSV files such as `filtered_reviews_1percent.csv`, `filtered_reviews_10percent.csv`, and `filtered_reviews_100percent.csv`.
- `scripts/eda.ipynb` has the data discovery section refered in `scripts/Part_1.ipynb`
- `scripts/Part_2_BERT.py`, `scripts/Part_3_BERT.py`: script versions of the BERT experiments.
- `scripts/Part_1.ipynb`, `scripts/Part_2_Augmentation.ipynb`, `scripts/Part_3.ipynb`, `scripts/Part_4_distillation_quantization.ipynb`: notebook versions of the project stages.
- `data/`: input data, cached checkpoints, and experiment outputs.
- `models/`: saved teacher/student checkpoints.
- `results/`: JSON, CSV, and NPZ outputs from the evaluation pipeline.
- `figures/`: plots and diagrams generated by the notebooks.

## Setup

Install dependencies with:

```bash
uv sync
```

If you are using a fresh environment, this repo expects Python 3.10 or newer and the packages listed in `pyproject.toml`.

## Data Preparation

Run the dataset preparation scripts in this order:

```bash
python scripts/setup.py
python scripts/setup_part3.py
```

`setup.py` prepares the base Trustpilot dataset and the initial filtered dataset which truncates the dataset to 32 rows needed for Part 2. 
`setup_part3.py` then creates the Part 3 training subsets [ 1%, 10%, 25%, 50%, 75%, 100% of original dataset] used for the learning-curve experiments.

## Running the Project

Recommended order:

1. Run `scripts/Part_1.ipynb` for the initial analysis.
2. Run `scripts/Part_2_Augmentation.ipynb` or `scripts/Part_2_BERT.py` for augmentation and BERT experiments.
3. Run `scripts/Part_3.ipynb` or `scripts/Part_3_BERT.py` for the full-data learning curve.
4. Run `scripts/Part_4_distillation_quantization.ipynb` for quantization, distillation, and error analysis.

## Outputs

The notebooks and scripts write their generated artifacts into the repository folders listed above. In particular:

- `data/results_checkpoint_BERT*.json` stores Part 3 learning-curve checkpoints.
- `results/part4_*.json` and `results/part4_*.csv` store Part 4 evaluation summaries.
- `models/` stores saved Hugging Face checkpoints for the teacher and student models.

## Notes

- The current Part 4 teacher checkpoint is `models/best_bert_100/`.
- If you rerun Part 4 after changing models or splits, regenerate the saved JSON files in `results/` so the reports stay consistent.